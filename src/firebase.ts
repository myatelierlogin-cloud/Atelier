// Firebase has been replaced by Cloudflare Workers + D1 + R2 + KV.
// This stub prevents stale imports from crashing the build.
// Remove any remaining imports of this file and use src/lib/api.ts instead.
export const db = null as any
export const auth = null as any
export const storage = null as any
